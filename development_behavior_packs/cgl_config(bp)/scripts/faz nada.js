// import { system, world } from "@minecraft/server";

// let blockCount = null

// world.afterEvents.playerBreakBlock.subscribe((event)=>{
//     const aa = event.block

//     blockCount= blockCount + 1
// })

// system.runInterval(() => {
//     world.getPlayers().forEach((player) => {
//         player.onScreenDisplay.setTitle(
//             `xubis:${blockCount}`
//         )
//     })
// })